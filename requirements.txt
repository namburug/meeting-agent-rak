fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
python-multipart==0.0.9
dateparser==1.2.0
pydantic==2.9.2

# LLM providers for the extraction step — all three installed so LLM_PROVIDER
# in .env can be switched between anthropic / openai / google without reinstalling.
anthropic>=0.40.0
openai>=1.40.0
google-genai>=0.3.0
